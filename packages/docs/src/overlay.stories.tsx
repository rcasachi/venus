import React from "react";
import { Overlay } from "@venusui/components";

export default { title: 'Overlay' };

export const overlays = () => (
  <Overlay css={{ width: '100vh', height: '50vh'}} />
);